# Prettier Rule

- Always ensure that code modifications adhere to the project's Prettier configuration (`.prettierrc.json`).
- After creating or modifying files, run `npm run format -- <path_to_file>` or `npx prettier --write <path_to_file>` to ensure consistent styling.
- Do not ask the user for permission to format; just do it as part of the task.
