services:
    db:
        image: postgres:16-alpine
        environment:
            POSTGRES_USER: postgres
            POSTGRES_PASSWORD: postgres
            POSTGRES_DB: typesql_example
        ports:
            - "54333:5432"
        volumes:
            - typesql_example_pgdata:/var/lib/postgresql/data

volumes:
    typesql_example_pgdata:
